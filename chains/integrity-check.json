{
    "version": 1,
    "name": "Hash + hex preview",
    "description": "Fan-out example: compute a file's SHA-256 and a hex dump of its bytes, side by side.",
    "nodes": [
        { "id": "sha", "tool": "hash", "options": { "algorithm": "sha256" } },
        { "id": "hex", "tool": "hex-encode", "options": {} }
    ],
    "edges": []
}
